# JVM 从字节码到 GC：入门学习笔记

> 学习目标：看懂 Java 程序从 `.java` 到运行、再到垃圾回收的大致过程；能区分类加载、运行时内存区域、执行引擎和 GC。

---

## 1. 先建立全局地图

```text
Hello.java
   │  javac（编译）
   ▼
Hello.class（字节码）
   │  JVM：类加载 → 运行时内存 → 执行引擎执行 → GC 回收垃圾
   ▼
程序运行结果
```

JVM（Java Virtual Machine，Java 虚拟机）可以理解为运行 Java 字节码的运行环境。

Java 的常见口号是“**一次编译，到处运行**”。原因不是 `.java` 源码能直接到处跑，而是：

1. Java 源码先被编译成统一格式的 **字节码**（`.class`）；
2. Windows、macOS、Linux 等平台分别安装自己的 JVM；
3. 各平台 JVM 负责执行同一份字节码。

因此，**跨平台的是字节码；真正执行字节码的是各平台上的 JVM。**

---

## 2. `.java`、`.class` 与字节码

### 2.1 编译过程

例如：

```java
public class Hello {
    public static void main(String[] args) {
        System.out.println("hello");
    }
}
```

执行：

```text
javac Hello.java
```

会生成：

```text
Hello.class
```

`.class` 文件不是机器码，而是 **Java 字节码（bytecode）**。它是一种 JVM 能理解的中间指令格式。

### 2.2 字节码的作用

字节码主要解决两件事：

- 让 Java 程序不必为每一种操作系统、CPU 各写一份；
- 让 JVM 可以在运行时做校验、解释执行、即时编译等优化。

可以用这个类比：

```text
Java 源码   = 人写的剧本
字节码      = 统一格式的演出指令
JVM         = 各地的剧团，负责理解并演出指令
机器码      = 最终由 CPU 执行的指令
```

---

## 3. JVM 的核心组成

通常可粗略分成三部分：

```text
JVM
├── 类加载子系统：把 .class 加载进 JVM
├── 运行时数据区：程序运行时使用的内存
└── 执行引擎：执行字节码；包含解释器、JIT、GC 等
```

这三部分的顺序也很好记：

```text
先加载类 → 再分配/使用内存 → 再执行代码并回收无用对象
```

---

## 4. 类加载子系统

### 4.1 类加载在做什么

类加载子系统负责把 `.class` 文件中的字节码读取到 JVM 中，并把它变成 JVM 可以使用的 `Class` 对象。

例如：

```java
Class<?> clazz = Class.forName("com.example.Student");
```

这行代码会要求 JVM 加载 `Student` 类；反射正是建立在 `Class` 对象之上的。

### 4.2 类加载的五个阶段

```text
加载 → 验证 → 准备 → 解析 → 初始化
```

| 阶段 | 做什么 | 记忆方式 |
| --- | --- | --- |
| 加载 | 找到并读取 `.class`，生成 `Class` 对象 | 把书拿进来 |
| 验证 | 检查字节码是否符合 JVM 规则、是否安全 | 检查书有没有问题 |
| 准备 | 为类变量分配内存并赋默认值 | 先摆好空白表格 |
| 解析 | 把符号引用转换为直接引用 | 把名字对应到真实目标 |
| 初始化 | 执行静态代码块、给静态变量赋程序员写的值 | 真正开始设置内容 |

注意：**准备阶段是默认值，初始化阶段才是代码中的实际赋值。**

```java
public static int number = 10;
```

可以粗略理解为：

```text
准备阶段：number = 0
初始化阶段：number = 10
```

### 4.3 类加载器

常见的类加载器有三类：

| 类加载器 | 主要加载内容 |
| --- | --- |
| 启动类加载器（Bootstrap ClassLoader） | Java 核心类库，如 `java.lang.String` |
| 平台类加载器（Platform ClassLoader） | 平台相关或扩展类库 |
| 应用类加载器（AppClassLoader） | 自己写的项目代码、classpath 中的类 |

### 4.4 双亲委派模型

加载一个类时，通常不是当前加载器立刻自己加载，而是先交给上层加载器尝试：

```text
应用类加载器
     ↓ 先问上级
平台类加载器
     ↓ 先问上级
启动类加载器
```

如果上层找不到，再由下层自己加载。

好处：

- 避免同一个类被重复加载；
- 保护核心类。例如不能轻易自己写一个 `java.lang.String` 来替换 JVM 原本的 `String`。

---

## 5. 运行时数据区：JVM 内存地图

程序运行时，JVM 会划分不同区域存放不同数据。

```text
运行时数据区
├── 程序计数器（PC Register）      每个线程独有
├── 虚拟机栈（Java Virtual Machine Stack） 每个线程独有
├── 本地方法栈（Native Method Stack） 每个线程独有
├── 堆（Heap）                     所有线程共享
└── 方法区（Method Area）           所有线程共享
```

最重要的先记一句：

```text
对象主要在堆中；方法执行过程主要在栈中。
```

### 5.1 程序计数器

程序计数器可以理解为每个线程自己的“执行进度条”，记录当前线程下一条要执行的字节码指令位置。

多线程轮流获得 CPU 时间时，每个线程都需要保存自己的位置，所以它是**线程私有**的。

### 5.2 Java 虚拟机栈

每个线程启动时都会有自己的虚拟机栈；每调用一个 Java 方法，就会创建一个**栈帧**。

```text
main() 栈帧
  └── methodA() 栈帧
        └── methodB() 栈帧
```

方法结束，对应栈帧就出栈。

一个栈帧通常包含：

- 局部变量表：方法参数、局部变量；
- 操作数栈：计算过程的临时数据；
- 动态链接：帮助定位要调用的方法；
- 方法返回地址：方法执行完回到哪里。

常见错误：

```text
StackOverflowError
```

通常表示递归太深或方法调用层级过深，栈空间不够。

### 5.3 本地方法栈

本地方法栈服务于 `native` 方法，也就是由 C/C++ 等非 Java 语言实现、Java 通过 JNI 调用的方法。

初学阶段只要记住：**Java 方法主要用虚拟机栈，本地方法主要和本地方法栈有关。**

### 5.4 堆

堆是 JVM 中通常最大的内存区域，主要用于存放：

- `new` 出来的对象；
- 数组；
- 对象的实例字段。

```java
Student student = new Student("贺博", 20);
```

粗略理解：

```text
student 这个局部变量（引用） → 栈
new Student(...) 创建的对象     → 堆
```

堆是所有线程共享的，也是垃圾回收器重点管理的区域。

### 5.5 方法区

方法区用于存储类相关信息，例如：

- 类的结构信息；
- 方法信息；
- 字段信息；
- 静态变量；
- 运行时常量池。

在 HotSpot JVM 的较新实现中，方法区的具体落地实现通常称为 **元空间（Metaspace）**，使用本地内存。

---

## 6. 对象从创建到使用：一次完整追踪

看这段代码：

```java
public static void main(String[] args) {
    Student student = new Student("贺博", 20);
    student.sleep();
}
```

可以按下面理解：

1. JVM 需要 `Student`，若尚未加载，就通过类加载子系统加载它；
2. `main` 方法执行，创建 `main` 栈帧；
3. `new Student(...)` 在堆中创建 `Student` 对象；
4. 局部变量 `student` 在 `main` 的局部变量表中保存对象引用；
5. 调用 `student.sleep()`，栈中创建 `sleep` 方法对应的栈帧；
6. `sleep` 返回，栈帧出栈；
7. `main` 返回，`main` 栈帧出栈；
8. 当对象不再被任何可达引用使用时，未来某次 GC 可以回收它。

---

## 7. 执行引擎：JVM 怎样执行字节码

执行引擎负责把字节码真正执行起来，重点有：

```text
执行引擎
├── 解释器
├── JIT 即时编译器
└── 垃圾回收器（GC）
```

### 7.1 解释器

解释器可以理解为“读一条字节码、执行一条”。启动快，但同一段热点代码反复执行时效率较低。

### 7.2 JIT 即时编译器

JIT（Just-In-Time Compiler）会识别经常执行的热点代码，把它编译成更接近机器码的形式，后续运行更快。

```text
少量执行：解释器直接执行，启动快
反复执行：JIT 编译热点代码，后续更快
```

所以 Java 既不是“永远纯解释执行”，也不是“一开始全部编译成机器码”；通常是解释器与 JIT 配合。

---

## 8. 什么是垃圾回收（GC）

GC（Garbage Collection）负责自动回收堆中不再使用的对象内存。

例如：

```java
Student student = new Student("贺博", 20);
student = null;
```

当这个对象不再能从程序仍在使用的引用链中访问到时，它就可能成为垃圾对象，之后由 GC 在合适时机回收。

注意：

```java
System.gc();
```

只是“建议 JVM 做一次 GC”，不保证立刻执行。

### 8.1 如何判断对象是不是垃圾：可达性分析

现代 JVM 的核心思路是**可达性分析**：从一组 GC Roots 出发，看对象是否还能被找到。

```text
GC Roots → 对象 A → 对象 B
```

若一个对象从任何 GC Roots 都无法到达，它通常就可以被回收。

常见 GC Roots 可先记：

- 栈帧局部变量表中引用的对象；
- 类的静态变量引用的对象；
- JNI 本地方法引用的对象；
- 正在运行的线程相关对象。

### 8.2 为什么不用“引用计数法”作为主方法

引用计数法会记录每个对象被引用几次，次数为 0 就回收；它简单，但处理不了循环引用：

```text
A 引用 B，B 引用 A
外部已经没有任何引用指向 A、B
```

两者计数都不是 0，却已经没有用了。可达性分析可以解决这类情况。

---

## 9. 常见垃圾回收算法

### 9.1 标记—清除（Mark-Sweep）

步骤：

1. 标记哪些对象是垃圾；
2. 直接清除垃圾对象。

优点：简单。

缺点：会产生大量内存碎片；分配大对象时可能找不到连续空间。

### 9.2 复制（Copying）

把内存分成两块，只使用其中一块；回收时把仍存活的对象复制到另一块，然后一次清空原区域。

优点：没有内存碎片，分配速度快。

缺点：需要预留一部分内存；存活对象多时复制成本高。

常适合：对象朝生夕灭、存活率低的区域。

### 9.3 标记—整理（Mark-Compact）

步骤：

1. 标记存活对象；
2. 把存活对象向一端移动；
3. 清理边界以外空间。

优点：没有碎片，不需要额外对半内存。

缺点：移动对象有成本。

常适合：存活对象较多的区域。

---

## 10. 分代收集与常见 GC 名称

JVM 常把堆按对象生命周期特点分区管理，常见概念：

```text
堆
├── 新生代（Young Generation）
│   ├── Eden
│   ├── Survivor 0
│   └── Survivor 1
└── 老年代（Old Generation）
```

新建对象通常先进入新生代；经历多次回收仍存活的对象，可能晋升到老年代。

| 名称 | 大致含义 |
| --- | --- |
| Minor GC / Young GC | 主要回收新生代，通常较频繁、较快 |
| Major GC | 常指与老年代相关的回收；不同资料语义可能略有差异 |
| Full GC | 通常影响范围更大，停顿往往更明显，应重点关注 |

常见收集器名称只需先知道定位：

| 收集器 | 入门理解 |
| --- | --- |
| Serial GC | 单线程收集，简单，适合较小内存或单核场景 |
| Parallel GC | 关注吞吐量，使用多线程并行回收 |
| CMS | 以降低停顿为目标的老收集器，已被逐步淘汰/替代 |
| G1 GC | 面向较大堆、可预测停顿目标的现代收集器 |

初学时不要死背所有收集器细节。先掌握：**GC 回收堆内存；不同算法和收集器是在吞吐量、停顿时间、内存占用之间做取舍。**

---

## 11. 高频概念辨析

| 概念 | 不要混淆成 |
| --- | --- |
| `.java` | 人写的源码，不是 JVM 直接执行的文件 |
| `.class` | JVM 读取的字节码，不等于 CPU 机器码 |
| JVM | 执行字节码的虚拟机，不等于 JDK |
| JDK | Java 开发工具包，包含编译器、运行环境等开发工具 |
| 堆 | 主要存对象和数组，不是所有变量都在堆 |
| 栈 | 主要服务方法调用，保存栈帧和局部变量等 |
| 方法区 / 元空间 | 保存类相关信息，不等于堆中普通对象 |
| `void` 方法的 `Method.invoke` | 调用成功后返回 `null` |

---

## 12. 与你正在学的反射如何连接

反射的核心入口是 `Class` 对象。

```java
Class<?> clazz = Class.forName("MyReflection4.Student");
```

这说明：JVM 已加载或将要加载 `Student`，并让你拿到描述这个类的 `Class` 对象。

之后可以从 `Class` 获取：

```java
Field field = clazz.getDeclaredField("name");
Method method = clazz.getDeclaredMethod("eat", String.class);
Constructor<?> constructor = clazz.getDeclaredConstructor(String.class, int.class);
```

这三者分别描述：

```text
Field       → 成员变量
Method      → 成员方法
Constructor → 构造器
```

若成员是 `private`，反射访问前通常需要：

```java
field.setAccessible(true);
method.setAccessible(true);
constructor.setAccessible(true);
```

---

## 13. 一页复习版

```text
1. Java 源码通过 javac 编译为 .class 字节码。
2. JVM 执行字节码，因此同一份字节码可在不同平台的 JVM 上运行。
3. JVM 核心：类加载子系统、运行时数据区、执行引擎。
4. 类加载：加载 → 验证 → 准备 → 解析 → 初始化。
5. 对象通常在堆中；方法调用过程主要在虚拟机栈中。
6. 每个线程有自己的程序计数器、虚拟机栈、本地方法栈。
7. 堆和方法区由线程共享。
8. 执行引擎由解释器和 JIT 配合执行字节码。
9. GC 通过可达性分析判断对象是否可回收。
10. 常见回收算法：标记清除、复制、标记整理。
11. Minor GC 主要针对新生代；Full GC 往往范围更大、停顿更明显。
12. 反射以 Class 为入口，Field/Method/Constructor 分别代表字段、方法、构造器。
```

---

## 14. 自测题

1. `Hello.java` 经过 `javac` 后得到什么文件？
2. 为什么 Java 常说“一次编译，到处运行”？
3. 类加载的五个阶段分别是什么？准备阶段和初始化阶段有什么区别？
4. 堆和虚拟机栈各主要存什么？
5. 为什么每个线程都需要自己的程序计数器？
6. 解释器和 JIT 分别解决什么问题？
7. GC 怎样判断一个对象是否还能使用？
8. 标记清除、复制、标记整理各有什么特点？
9. `getDeclaredMethod("eat", String.class)` 中的 `String.class` 表示什么？
10. `void` 方法通过 `Method.invoke(...)` 调用后为什么会得到 `null`？

### 自测答案（简版）

1. `Hello.class` 字节码文件。
2. 同一字节码可交给不同平台上的 JVM 执行。
3. 加载、验证、准备、解析、初始化；准备赋默认值，初始化执行实际赋值和静态代码。
4. 堆主要放对象与数组；虚拟机栈主要服务方法调用，保存栈帧、局部变量等。
5. 多线程切换时，每个线程都要记住自己的下一条执行位置。
6. 解释器启动快；JIT 把热点代码编译优化，让反复执行更快。
7. 从 GC Roots 出发做可达性分析。
8. 标记清除简单但有碎片；复制无碎片但要复制存活对象；标记整理无碎片但移动对象有成本。
9. `eat` 方法的参数类型。
10. 因为 `void` 方法本来没有返回值，反射调用后以 `null` 表示无返回值。

---

## 15. 面试补强：类加载器的完整问题

### 15.1 为什么需要类加载器

JVM 不会在程序启动时把所有 `.class` 文件一次性全部放进内存，而是通常在“第一次真正需要某个类”时再加载。这样能减少启动和内存压力。

同一个类在 JVM 中的身份不仅由**全限定类名**决定，还与**加载它的类加载器**有关：

```text
类的身份 = 全限定类名 + 定义它的类加载器
```

所以，两个不同类加载器加载的同名类，JVM 可以把它们当成不同类型。

### 15.2 双亲委派的完整加载逻辑

应用类加载器收到加载 `com.example.Student` 的请求时，简化流程为：

```text
1. 这个加载器以前加载过它吗？加载过就直接返回。
2. 没加载过，先请求父加载器加载。
3. 父加载器继续向上请求，直到启动类加载器。
4. 上层找得到，就直接使用上层加载结果。
5. 上层都找不到，当前加载器再自己尝试加载。
```

这不是“父加载器一定是 Java 继承关系中的父对象”，而是一种委托关系。

### 15.3 什么时候会打破双亲委派

不是所有场景都严格遵循“先父后子”。典型原因：

- **SPI**：核心库要加载第三方实现，例如 JDBC 驱动；
- **Tomcat**：一个服务器要同时部署多个 Web 应用，不同应用可能依赖同名但不同版本的库；
- **热部署/插件化**：需要动态加载、卸载或隔离模块。

### 15.4 Tomcat 为什么需要自定义类加载器

Tomcat 同时运行多个 Web 应用时，希望：

1. 每个应用使用自己版本的依赖，互不冲突；
2. 多个应用可共享 Servlet API 等公共库；
3. 应用重新部署时，旧类能够随旧类加载器一起被卸载。

因此 Tomcat 会用多层类加载器进行隔离和共享。这是“自定义类加载器”的经典面试场景。

---

## 16. 面试补强：对象是怎样创建出来的

代码：

```java
Student student = new Student("贺博", 20);
```

可以按下面六步理解：

1. **检查类是否已加载**：`Student` 未加载则先加载、连接、初始化；
2. **分配内存**：在堆中为对象所需字段分配空间；
3. **默认初始化**：字段先有零值，例如 `int` 为 `0`、引用为 `null`；
4. **设置对象头**：写入对象所属类、锁状态、哈希等运行时信息；
5. **执行构造器**：执行 `Student("贺博", 20)`，把字段改成实际值；
6. **将引用赋给变量**：局部变量 `student` 保存新对象的引用。

### 16.1 对象内存布局（知道概念即可）

HotSpot 中对象可粗略分为：

```text
对象头 + 实例数据 + 对齐填充
```

- 对象头：运行时元数据、锁相关信息、类型指针等；
- 实例数据：`name`、`age` 等字段实际值；
- 对齐填充：让对象大小满足内存对齐要求。

---

## 17. 面试补强：栈、堆与常见异常

### 17.1 `StackOverflowError` 和 `OutOfMemoryError`

| 现象 | 常见原因 |
| --- | --- |
| `StackOverflowError` | 递归没有正确终止，或调用层数过深，栈帧堆积 |
| `OutOfMemoryError: Java heap space` | 堆不够，或对象被意外长期持有导致无法回收 |
| `OutOfMemoryError: Metaspace` | 动态生成/加载过多类，类加载器无法释放等 |

### 17.2 为什么局部变量不一定立刻让对象回收

对象能否回收取决于是否还能从 GC Roots 到达，而不是只看某一行代码“看起来不用了”。JIT 可能优化局部变量的存活范围；垃圾回收的准确时机也由 JVM 决定。

---

## 18. 垃圾回收的详细流程

### 18.1 新生代为何适合复制算法

大部分对象“朝生夕灭”：创建后很快不再使用。因此新生代存活对象通常较少，复制少量存活对象、清空大块区域非常划算。

简化流程：

```text
Eden 满了
  ↓
Minor GC
  ↓
存活对象复制到 Survivor 区
  ↓
年龄增加；达到条件的对象晋升到老年代
  ↓
清空 Eden（以及无用 Survivor）
```

### 18.2 为什么老年代常用标记整理

老年代对象存活率通常更高，若使用复制算法，需要复制大量对象且要预留很大空闲区，成本较高；标记整理更合适。

### 18.3 Stop-The-World（STW）

很多 GC 阶段需要暂停所有业务线程，这叫 **Stop-The-World**。

STW 不代表 JVM 崩溃，而是为了保证回收时对象引用关系不会持续变化。不同收集器的核心差异之一，就是如何降低 STW 的频率或时间。

---

## 19. 常见垃圾收集器对比（面试版）

> 以下是理解定位用的简化比较。不同 JDK 版本的默认收集器和支持状态会变化，实际使用以目标 JDK 文档和压测为准。

| 收集器 | 特点 | 更关注什么 | 关键短语 |
| --- | --- | --- | --- |
| Serial | 单线程回收，简单 | 小内存、简单场景 | 单线程、STW |
| Parallel Scavenge / Parallel Old | 多线程并行回收 | 吞吐量 | 高吞吐量 |
| CMS | 并发标记清除，老年代收集器 | 降低停顿 | 并发、碎片、已淘汰趋势 |
| G1 | 将堆划为多个 Region，分阶段回收 | 可预测停顿 | Region、停顿目标 |

### 19.1 Serial GC

- 单个 GC 线程完成回收；
- GC 时业务线程暂停；
- 实现简单，但大堆或多核机器下不占优势。

### 19.2 Parallel GC

- 使用多个 GC 线程并行回收；
- 更关注单位时间内完成多少工作，即**吞吐量**；
- 常见指标：

```text
吞吐量 = 用户代码运行时间 / （用户代码运行时间 + GC 时间）
```

### 19.3 CMS

CMS（Concurrent Mark Sweep）主打降低老年代回收时的停顿，通过并发标记、并发清除让 GC 线程与业务线程部分同时运行。

它的典型问题：

- 使用标记清除，容易产生内存碎片；
- 并发回收会占用 CPU；
- 可能出现并发回收期间内存仍不足的情况；
- 在较新 JDK 中已被移除，学习价值主要在理解“低停顿”和“碎片”的取舍。

### 19.4 G1

G1（Garbage-First）把堆划分为许多大小相同的 **Region**，不再机械地只按连续的新生代/老年代块来管理。

其入门关键点：

- 每个 Region 可以在不同时间承担 Eden、Survivor、Old 等角色；
- 优先回收垃圾多、收益高的 Region；
- 可设定期望最大停顿时间，例如 `-XX:MaxGCPauseMillis`；
- “可预测”不等于绝对保证，而是尽力满足停顿目标。

### 19.5 如何回答“如何选 GC”

不要机械回答“G1 永远最好”。更好的回答方式：

1. 看 JDK 版本和默认收集器；
2. 看应用目标：吞吐量优先，还是延迟/停顿优先；
3. 看堆大小、对象生命周期和 GC 日志；
4. 在真实压测下评估，不凭感觉调参数。

---

## 20. GC 高频面试问答

### Q1：对象一定在堆上吗？

入门回答：通常把对象理解为在堆上。进阶补充：JIT 可能做逃逸分析和标量替换等优化，实际实现不一定严格按源码直觉分配；但学习内存区域时，先掌握“对象主要在堆中”即可。

### Q2：`new` 了对象一定会立刻占用老年代吗？

不会。通常先进入新生代 Eden；经过多次 Minor GC 仍存活、或满足某些条件，才可能晋升老年代。大对象、年龄阈值等具体策略受 JVM 参数影响。

### Q3：Minor GC 一定比 Full GC 快吗？

通常是。Minor GC 范围较小、对象存活率通常低；Full GC 涉及范围往往更大，停顿风险更高。但最终要看堆大小、对象存活情况和收集器。

### Q4：为什么 Full GC 要尽量少？

它通常意味着更大范围的回收和更明显停顿，可能影响接口延迟、吞吐量和用户体验。排查时要看 GC 日志、堆使用情况、对象持有链和参数配置。

### Q5：内存泄漏和内存溢出有什么区别？

- **内存泄漏**：本该不再使用的对象仍被引用，GC 无法回收；
- **内存溢出（OOM）**：可用内存不够，分配失败。

内存泄漏长期积累，常常最终导致 OOM；但 OOM 不一定都是内存泄漏，也可能只是配置太小或瞬时负载过高。

---

## 21. 把本章内容串成一条“面试回答链”

当被问“Java 程序如何运行”时，可以按这条链回答：

```text
Java 源码先经 javac 编译成 .class 字节码。
程序运行时，JVM 的类加载器按委派机制加载所需类，经历加载、验证、准备、解析、初始化。
类加载后，方法调用在线程私有的虚拟机栈中形成栈帧；new 出来的对象主要分配在堆中；类元数据等位于方法区/元空间。
执行引擎先用解释器执行字节码，对热点代码可由 JIT 编译优化。
堆中不可从 GC Roots 到达的对象会被 GC 回收；新生代常适合复制算法，老年代常考虑标记整理等策略。
不同收集器在吞吐量、停顿时间、CPU 和内存开销之间做取舍。
```

---

## 22. 学完这一节后，应该能独立回答什么

- 为什么 Java 能跨平台？
- `javac` 做了什么？`.class` 是什么？
- JVM 包含哪些核心部分？
- 类加载五个阶段是什么？双亲委派有什么价值？
- 栈、堆、方法区分别放什么？
- 为什么递归过深会 `StackOverflowError`？
- 对象如何从创建到成为可回收垃圾？
- GC Roots 和可达性分析是什么？
- 三种垃圾回收算法有什么优缺点？
- Minor GC、Full GC、Parallel GC、CMS、G1 分别是什么定位？
- 反射为什么要先获取 `Class`，以及 `Field`、`Method`、`Constructor` 分别做什么？
